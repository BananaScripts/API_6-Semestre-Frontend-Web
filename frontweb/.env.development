# Development environment variables for frontweb
# Set to the relative path for the login route so the dev proxy handles the request
REACT_APP_LOGIN_ROUTE=/usuario
# If the backend expects form-encoded body set this to "true". Default: false (send JSON)
REACT_APP_USE_FORM=false
REACT_APP_FRONTEND_AUTH=true
